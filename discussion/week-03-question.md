Choosing a step size for gradient descent involves a tradeoff: too small and convergence is slow, too large and the algorithm can diverge entirely. Outside of computing exact eigenvalue bounds (which requires knowing the curvature in advance, itself expensive for large problems), what practical strategies exist for picking a reasonable step size, and what signs would tell you your choice is too aggressive before you've wasted a lot of computation?