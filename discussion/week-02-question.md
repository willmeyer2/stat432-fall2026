"In Homework 2, adding one predictor improved the fit slightly, but Cp/AIC favored including it while BIC favored excluding it — because BIC penalizes each extra parameter roughly 3x more heavily (log(n) vs. 2) for n=370. As a dataset grows larger, BIC's penalty keeps growing (log(n) increases), while Cp/AIC's stays fixed at 2. What does this suggest about how these criteria behave differently as sample size grows, and is a stricter penalty always the 'safer' choice?"